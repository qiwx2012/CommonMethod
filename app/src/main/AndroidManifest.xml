<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="test.com.commonmethod">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name="list.StudentTest">


        </activity>

        <activity android:name=".ObjChangeAct"></activity>
        <activity android:name=".DatePickAct"></activity>
        <activity android:name=".ActContainer"></activity>
        <activity android:name=".ActBindService"></activity>
        <service android:name="thread.LocalIntentServcice"></service>
        <service android:name="thread.BindService"></service>
        <activity android:name=".TestAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>